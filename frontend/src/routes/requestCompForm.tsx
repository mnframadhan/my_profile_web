import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/requestCompForm')({
  component: RequestCompForm
})

function RequestCompForm() {
  return (
    <>
    </>
  )
}